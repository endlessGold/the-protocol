[package]
name = "protocol-presentation"
version = "0.1.0"
edition = "2021"

[dependencies]
serde.workspace = true

protocol-domain = { path = "../../domain" }

[dev-dependencies]
# Only for the tests that pin the exact JSON shape the Godot client parses.
serde_json.workspace = true
